components:
  schemas:
    UserInsert:
      required:
      - name
      - email
      - company_id
      type: object
      properties:
        name:
          type: string
        email:
          type: string
          format: email
        company_id:
          type: integer
          format: int32
        role_id:
          type: integer
          format: int32