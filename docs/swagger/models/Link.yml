components:
  schemas:
    Link:
      type: object
      properties:
        id:
          type: integer
          format: int32
        name:
          type: string
        link:
          type: string
        category_id:
          type: integer
          format: int32
        active:
          type: boolean
        created_by:
          type: integer
          format: int32
        updated_by:
          type: integer
          format: int32
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
